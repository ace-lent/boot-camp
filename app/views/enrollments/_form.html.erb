<%= form_with(model: enrollment_record, local: true) do |form| %>
  <% if enrollment_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enrollment_record.errors.count, "error") %> prohibited this enrollment record from being saved:</h2>

      <ul>
        <% enrollment_record.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.all, :id, :name, prompt: true %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.collection_select :course_id, Course.all, :id, :title, prompt: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
